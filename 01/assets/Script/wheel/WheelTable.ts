import { _decorator, Component, Node, Label, math, v2 } from 'cc';
const { ccclass, property } = _decorator;

@ccclass('WheelTable')
export class WheelTable extends Component {


    start() {
    }

    update(deltaTime: number) {
    }
}

